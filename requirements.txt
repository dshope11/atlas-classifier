# Core ML stack — versions pinned to known-good as of 2026-04-25
torch>=2.1
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
pandas>=2.0

# HEP / data I/O
uproot>=5.0
awkward>=2.5
h5py>=3.10
atlasopenmagic>=1.9     # sample/URL discovery for ATLAS Open Data 2025 release
aiohttp>=3.10           # async HTTPS streaming for uproot

# Plotting
matplotlib>=3.8
seaborn>=0.13

# Config + utilities
pyyaml>=6.0
tqdm>=4.65

# Optional: hyperparameter optimisation (Stage: scripts/tune.py)
optuna>=3.5

# Optional: BDT comparison (Stage: src/bdt.py)
xgboost>=2.0

# Dev tooling (linting + tests run via .claude/settings.json post-edit hook)
ruff>=0.4
mypy>=1.8
pytest>=8.0
types-PyYAML>=6.0
types-requests>=2.31
types-tqdm>=4.65
